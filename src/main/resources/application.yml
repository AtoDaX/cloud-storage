server:
  port: 8080

spring:
  thymeleaf:
    cache: false
  datasource:
    name: cloudstorageh2
    generate-unique-name: false
  session:
    redis:
      save-mode: always
  data:
    redis:
      host: localhost
      password:
      port: 6379

